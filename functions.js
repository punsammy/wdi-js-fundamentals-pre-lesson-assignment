// Named function
function eatDot() {
  console.log("MUNCH!");
}

// Anonymous function
// Here we assign the function to a variable instead of giving it a name directly
        // var eatDot = function() {
        //   console.log("MUNCH!");
        // }
eatDot()
// returns MUNCH!

// eatDot ==> Without brackets returns the actual function but doesnt execute it

var hero = 'Pac-Man'
hero.toUpperCase();

hero.length;
// This works because Length is property of the String type, not function of String type
